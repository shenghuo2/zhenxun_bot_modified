# MC版本检测插件配置示例
# 将此配置添加到你的 .env 文件中

# MC版本更新推送的群组ID列表（用逗号分隔）
# 例如：MCVER_GROUP_ID=["123456789", "987654321"]
# 如果不需要推送到任何群组，可以设置为空列表：MCVER_GROUP_ID=[]
MCVER_GROUP_ID=[]

# 注意：
# 1. 群组ID必须是字符串格式，用双引号包围
# 2. 多个群组ID用逗号分隔
# 3. 如果不配置此项，插件仍然可以正常工作，但不会推送更新消息