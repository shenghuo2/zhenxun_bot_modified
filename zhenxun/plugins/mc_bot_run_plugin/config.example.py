from pathlib import Path

# SSH connection settings. Copy to config.py locally and fill in real values.
SSH_HOST = "example.com"
SSH_USER = "minecraft"
SSH_KEY_PATH = Path(__file__).parent / "id_rsa"
